<?xml version="1.0" encoding="UTF-8"?>

<module xmlns="urn:jboss:module:1.3" name="org.jgroups.kubernetes" slot="${slot}">
    <properties>
        <property name="jboss.api" value="private"/>
    </properties>

    <resources>
        <!-- Insert resources here -->
    </resources>

    <dependencies>
        <module name="javax.api"/>
        <module name="net.oauth.core" slot="${slot}"/>
        <module name="org.jgroups" slot="${slot}"/>
    </dependencies>
</module>

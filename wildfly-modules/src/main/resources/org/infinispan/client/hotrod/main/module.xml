<?xml version="1.0" encoding="UTF-8"?>
<module xmlns="urn:jboss:module:1.3" name="org.infinispan.client.hotrod" slot="${slot}">
    <resources>
        <!-- Insert resources here -->
    </resources>

    <dependencies>
        <module name="javax.api"/>
        <module name="javax.transaction.api"/>
        <module name="io.netty" slot="${slot}"/>
        <module name="org.infinispan.commons" slot="${slot}"/>
        <module name="com.github.ben-manes.caffeine" slot="${slot}"/>
        <module name="org.infinispan.protostream" slot="${slot}"/>
        <module name="org.infinispan.query.dsl" slot="${slot}"/>
        <module name="org.infinispan.query.remote.client" slot="${slot}"/>
        <module name="org.jboss.logging"/>
    </dependencies>
</module>

[id='jcache']
:context: jcache
= Using the JCache (JSR-107) API

{brandname} provides a fully compliant implementation of the JCache (JSR-107) specification, the standard Java API for temporary caching.
You can use the JCache API as a portable caching layer that works across different cache providers.

By coding against the `javax.cache` API, your application remains portable across JCache-compliant implementations.

[IMPORTANT]
====
The JCache specification (JSR-107) is no longer under active development.
It was not part of Java EE, has not been adopted by Jakarta EE, and the expert group is no longer active.
While {brandname} continues to support the JCache API, no further evolution of the specification is expected.

For new projects, consider using the {brandname} native caching API or other actively maintained alternatives, which offer richer functionality and continued development.
====


include::{topics}/ref_jcache_dependencies.adoc[leveloffset=+1]
include::{topics}/proc_jcache_creating_caches.adoc[leveloffset=+1]
include::{topics}/con_jcache_read_write_through.adoc[leveloffset=+1]
include::{topics}/con_jcache_store_behavior.adoc[leveloffset=+1]

// Restore the parent context.
ifdef::parent-context[:context: {parent-context}]
ifndef::parent-context[:!context:]

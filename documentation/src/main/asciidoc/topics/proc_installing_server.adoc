[id='installing-servers_{context}']
= Installing {brandname} Server
Install the {brandname} Server distribution on a host system.

.Prerequisites

* Download a {brandname} Server distribution archive.

.Procedure

* Use any appropriate tool to extract the {brandname} Server archive to the host filesystem.

[source,options="nowrap",subs=attributes+]
----
include::cmd_examples/extract_server_distribution.adoc[]
----

The resulting directory is your `{server_home}`.

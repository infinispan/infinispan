[id='jgroups_transport-{context}']
= JGroups Transport
{brandname} clustered caches use JGroups as the transport layer.

JGroups transport declaration::
+
[source,xml,options="nowrap",subs=attributes+]
----
include::config_examples/ispn_jgroups_transport.xml[]
----

[id='manually_registering_sci-{context}']
= Manually Registering Serialization Context Initializers
You can disable automatic registration of `SerializationContextInitializer` implementations and then register them manually.

[IMPORTANT]
====
If you manually register one `SerializationContextInitializer` implementation, it disables automatic registration.
You must then manually register all other implementations.
====

.Procedure

. Set a value of `false` for the `AutoProtoSchemaBuilder.service` annotation.
+
[source,java]
----
@AutoProtoSchemaBuilder(
      includeClasses = SomeClass.class,
      ...
      service = false
)
----
+
. Manually register `SerializationContextInitializer` implementations, as in the following examples:

.Declarative
[source,xml,options="nowrap",subs=attributes+,role="primary"]
----
include::config_examples/protostream_marshaller.xml[]
----

.Programmatic
[source,java,options="nowrap",subs=attributes+,role="secondary"]
----
include::code_examples/ProtoStreamMarshaller.java[]
----

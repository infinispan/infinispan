<?xml version="1.0" encoding="UTF-8"?>
<assembly>
    <id>wildfly</id>
    <baseDirectory>${infinispan.brand.prefix}-${wildfly.brand.prefix}-modules-${infinispan.brand.version}</baseDirectory>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <fileSets>
        <fileSet>
            <directory>target/${infinispan.brand.prefix}-${wildfly.brand.prefix}-modules-${infinispan.brand.version}</directory>
            <outputDirectory/>
        </fileSet>
    </fileSets>
</assembly>
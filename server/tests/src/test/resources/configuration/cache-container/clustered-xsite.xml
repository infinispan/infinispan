<!-- Create a Cache Manager that exports statistics. -->
<cache-container xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="urn:infinispan:config:12.0 https://infinispan.org/schemas/infinispan-config-fragment-12.0.xsd"
                 xmlns="urn:infinispan:config:12.0"
                 name="default" statistics="true">
   <!-- Set up cluster transport with a JGroups stack. -->
   <transport cluster="${infinispan.cluster.name:cluster}" stack="${infinispan.cluster.stack:xsite-tcp}"/>
   <!-- Generate gauge and histogram metrics for the Cache Manager. -->
   <metrics gauges="true" histograms="true"/>
</cache-container>


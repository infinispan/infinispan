<!-- Configure endpoints to use socket bindings and security realms. -->
<endpoints xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="urn:infinispan:server:12.0 https://infinispan.org/schemas/infinispan-server-12.0.xsd"
           xmlns="urn:infinispan:server:12.0"
           socket-binding="default" security-realm="default">
   <!-- Configure Hot Rod and REST endpoint connectors to use token realms for authentication. Specify authentication mechanisms that each protocol supports. -->
   <hotrod-connector name="hotrod">
      <authentication>
         <sasl mechanisms="OAUTHBEARER" server-name="infinispan"/>
      </authentication>
   </hotrod-connector>
   <rest-connector name="rest">
      <authentication mechanisms="BEARER_TOKEN"/>
   </rest-connector>
</endpoints>

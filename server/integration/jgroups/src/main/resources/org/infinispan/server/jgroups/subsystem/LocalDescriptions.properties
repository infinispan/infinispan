jgroups-core=The configuration of the JGroups subsystem.
jgroups-core.add=Add the JGroups subsystem.
jgroups-core.remove=Remove the JGroups subsystem.
jgroups-core.describe=Describe the JGroups subsystem.
jgroups-core.default-channel=The default JGroups channel.
jgroups-core.default-stack=The default JGroups protocol stack.
jgroups-core.default-stack.deprecated=Deprecated. This attribute will be removed in a future release.
# stack resource
jgroups-core.stack=The configuration of a JGroups protocol stack.
jgroups-core.stack.add=Add a protocol stack to the JGroups subsystem.
jgroups-core.stack.add.transport=Optional OBJECT parameter specifying the transport for the stack.
jgroups-core.stack.add.transport.deprecated=Deprecated.  Use separate transport add operation instead.
jgroups-core.stack.add.transport.type=The implementation class for a protocol, which determines protocol functionality.
jgroups-core.stack.add.transport.module=The module with which to resolve the protocol type.
jgroups-core.stack.add.transport.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.stack.add.transport.properties=A list of properties for a protocol layer. Used to customize protocol behaviour.
jgroups-core.stack.add.transport.shared=If true, the underlying transport is shared by all channels using this stack.
jgroups-core.stack.add.transport.diagnostics-socket-binding=The diagnostics socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.stack.add.transport.default-executor=The thread pool executor to handle incoming messages.
jgroups-core.stack.add.transport.oob-executor=The thread pool executor to handle incoming out-of-band messages.
jgroups-core.stack.add.transport.timer-executor=The thread pool executor to handle protocol-related timing tasks.
jgroups-core.stack.add.transport.thread-factory=The thread factory to use for handling asynchronous transport-specific tasks.
jgroups-core.stack.add.transport.site=The site (i.e. data centre) identifier for this node. Used by Infinispan topology-aware consistent hash.
jgroups-core.stack.add.transport.rack=The rack (i.e. server rack) identifier for this node. Used by Infinispan topology-aware consistent hash.
jgroups-core.stack.add.transport.machine=The machine (i.e. host) identifier for this node. Used by Infinispan topology-aware consistent hash.
jgroups-core.stack.add.transport.property=A JGroups transport property.
jgroups-core.stack.add.protocols=Optional LIST parameter specifying the protocol list for the stack.
jgroups-core.stack.add.protocols.deprecated=Deprecated.  Use separate protocol add operations instead.
jgroups-core.stack.add.protocols.protocol.type=The implementation class for a protocol, which determines protocol functionality.
jgroups-core.stack.add.protocols.protocol.module=The module with which to resolve the protocol type.
jgroups-core.stack.add.protocols.protocol.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.stack.add.protocols.protocol.properties=Optional LIST parameter specifying the protocol list for the stack.
jgroups-core.stack.add.protocols.protocol.property=A JGroups protocol property.
jgroups-core.stack.remove=Remove a protocol stack from the jgroups subsystem.
jgroups-core.stack.add-protocol=Add the configuration of a protocol within a protocol stack.
jgroups-core.stack.add-protocol.deprecated=Deprecated use standard resource add operation instead.
jgroups-core.stack.add-protocol.type=The implementation class for a protocol, which determines protocol functionality.
jgroups-core.stack.add-protocol.type.deprecated=Deprecated. Will be removed in a future version.
jgroups-core.stack.add-protocol.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.stack.add-protocol.properties=Optional LIST parameter specifying the protocol list for the stack.
jgroups-core.stack.add-protocol.properties.deprecated=Deprecated. Use individual property add operations instead.
jgroups-core.stack.add-protocol.properties.property=A JGroups protocol property.
jgroups-core.stack.remove-protocol=Remove the configuration of a protocol within a protocol stack.
jgroups-core.stack.remove-protocol.deprecated=Deprecated use standard resource remove operation instead.
jgroups-core.stack.remove-protocol.type=The implementation class for a protocol, which determines protocol functionality.
jgroups-core.stack.remove-protocol.type.deprecated=Deprecated. Will be removed in a future version.
jgroups-core.stack.export-native-configuration=Export this stack configuration as a native JGroups format.
jgroups-core.stack.transport=A JGroups stack transport layer.
jgroups-core.stack.protocol=A JGroups stack protocol layer.
jgroups-core.stack.protocols=The list of configured protocols for a protocol stack.
jgroups-core.stack.protocols.protocol.type=Type of protocol
jgroups-core.stack.protocols.protocol.socket-binding=Optional socket binding for protocol
jgroups-core.stack.relay=A RELAY protocol
jgroups-core.stack.sasl=A SASL protocol
# transport resource
jgroups-core.transport=The configuration of a transport for a protocol stack.
jgroups-core.transport.add=Add the configuration of a transport for a protocol stack.
jgroups-core.transport.remove=Remove the configuration of a transport for a protocol stack.
jgroups-core.transport.type=The implementation class for a protocol, which determines protocol functionality.
jgroups-core.transport.type.deprecated=Deprecated. Will be removed in a future version.
jgroups-core.transport.module=The module with which to resolve the protocol type.
jgroups-core.transport.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.transport.shared=If true, the underlying transport is shared by all channels using this stack.
jgroups-core.transport.diagnostics-socket-binding=The diagnostics socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.transport.default-executor=The thread pool executor to handle incoming messages.
jgroups-core.transport.default-executor.deprecated=Deprecated. Supports EAP 6.x slaves.
jgroups-core.transport.oob-executor=The thread pool executor to handle incoming out-of-band messages.
jgroups-core.transport.oob-executor.deprecated=Deprecated. Supports EAP 6.x slaves.
jgroups-core.transport.timer-executor=The thread pool executor to handle protocol-related timing tasks.
jgroups-core.transport.timer-executor.deprecated=Deprecated. Supports EAP 6.x slaves.
jgroups-core.transport.thread-factory=The thread factory to use for handling asynchronous transport-specific tasks.
jgroups-core.transport.thread-factory.deprecated=Deprecated. Supports EAP 6.x slaves.
jgroups-core.transport.site=The site (i.e. data centre) identifier for this node. Used by Infinispan topology-aware consistent hash.
jgroups-core.transport.rack=The rack (i.e. server rack) identifier for this node. Used by Infinispan topology-aware consistent hash.
jgroups-core.transport.machine=The machine (i.e. host) identifier for this node. Used by Infinispan topology-aware consistent hash.
jgroups-core.transport.properties=Optional LIST parameter specifying the property list for the transport.
jgroups-core.transport.properties.deprecated=Deprecated. Use individual property add operations instead.
jgroups-core.transport.properties.property=A JGroups transport property.
jgroups-core.transport.property=A JGroups transport property.
# transport thread-factory resource
jgroups-core.transport.thread-pool=A thread pool executor
jgroups-core.transport.thread-pool.add=Adds a thread pool executor.
jgroups-core.transport.thread-pool.remove=Removes a thread pool executor.
jgroups-core.transport.thread-pool.min-threads=The core thread pool size which is smaller than the maximum pool size. If undefined, the core thread pool size is the same as the maximum thread pool size.
jgroups-core.transport.thread-pool.max-threads=The maximum thread pool size.
jgroups-core.transport.thread-pool.queue-length=The queue length.
jgroups-core.transport.thread-pool.keepalive-time=Used to specify the amount of milliseconds that pool threads should be kept running when idle; if not specified, threads will run until the executor is shut down.
# protocol resource
jgroups-core.protocol=The configuration of a protocol within a protocol stack.
jgroups-core.protocol.add=Add a protocol to a protocol stack.
jgroups-core.protocol.remove=Remove a protocol from a protocol stack.
jgroups-core.protocol.type=The implementation class for a protocol, which determines protocol functionality.
jgroups-core.protocol.type.deprecated=Deprecated. Will be removed in a future version.
jgroups-core.protocol.module=The module with which to resolve the protocol type.
jgroups-core.protocol.socket-binding=The socket binding specification for this protocol layer, used to specify IP interfaces and ports for communication.
jgroups-core.protocol.properties=The properties of this protocol.
jgroups-core.protocol.property=A JGroups protocol property.
# property resource
jgroups-core.property=A protocol property with name and value.
jgroups-core.property.deprecated=Deprecated. Protocol properties are defined via the "properties" attribute of the parent resource.
jgroups-core.property.add=Adds a protocol property.
jgroups-core.property.remove=Removes a protocol property.
jgroups-core.property.value=The value of a protocol property.
# channel resource
jgroups-core.channel=A JGroups channel.
jgroups-core.channel.add=Add a JGroups channel
jgroups-core.channel.remove=Remove a JGroups channel
jgroups-core.channel.stack=The protocol stack of the JGroups channel
jgroups-core.channel.module=The module from which to load channel services
jgroups-core.channel.address=The IP address of the channel.
jgroups-core.channel.address-as-uuid=The address of the channel as a UUID.
jgroups-core.channel.discard-own-messages=If true, do not receive messages sent by this node (ourself).
jgroups-core.channel.num-tasks-in-timer=The current number of timer tasks.
jgroups-core.channel.num-timer-threads=The number of timer threads.
jgroups-core.channel.received-bytes=The number of bytes received by this channel.
jgroups-core.channel.received-messages=The number of messages received by this channel.
jgroups-core.channel.sent-bytes=The number of bytes sent by this channel.
jgroups-core.channel.sent-messages=The number of messages sent by this channel.
jgroups-core.channel.state=The state of the channel (OPEN, CONNECTING, CONNECTED, CLOSED).
jgroups-core.channel.stats-enabled=If enabled, collect channel statistics.
jgroups-core.channel.version=The JGroups software version.
jgroups-core.channel.view=The channel's view of group membership.
jgroups-core.channel.protocol=A protocol instance within the channel's protocol stack.
jgroups-core.channel.fork=A JGroups channel fork
# fork resource
jgroups-core.fork=A JGroups channel fork
jgroups-core.fork.add=Add a JGroups channel fork
jgroups-core.fork.remove=Remove a JGroups channel fork
jgroups-core.fork.protocol=A JGroups channel fork protocol
# relay resource
jgroups-core.relay=The configuration of a RELAY protocol
jgroups-core.relay.add=Adds a RELAY protocol to this stack
jgroups-core.relay.remove=Removes the RELAY protocol from this stack
jgroups-core.relay.site=The name of the local site.
jgroups-core.relay.properties=An optional list of RELAY protocol properties.
jgroups-core.relay.property=A RELAY protocol property.
jgroups-core.relay.properties.property=A RELAY protocol property.
jgroups-core.relay.remote-sites=The remote sites to which to bridge.
jgroups-core.relay.remote-sites.remote-site=A remote site to which to bridge.
jgroups-core.relay.remote-site=A remote site to which to bridge.
# remote-site resource
jgroups-core.remote-site=A remote site to which to bridge.
jgroups-core.remote-site.add=Adds a remote site.
jgroups-core.remote-site.remove=Removes a remote site.
jgroups-core.remote-site.stack=The stack from which to create a bridge to this remote site.
jgroups-core.remote-site.stack.deprecated=Deprecated.  Use an explicitly defined channel instead.
jgroups-core.remote-site.cluster=The cluster name of the bridge channel to this remote site.
jgroups-core.remote-site.cluster.deprecated=Deprecated.  Use an explicitly defined channel instead.
jgroups-core.remote-site.channel=The name of the bridge channel used to communicate with this remote site.
# sasl resource
jgroups-core.sasl=The SASL protocol
jgroups-core.sasl.mech=The mech to use for the SASL protocol
jgroups-core.sasl.security-realm=The name of a security realm to be used by any realm-aware protocols contained within this stack
jgroups-core.sasl.cluster-role=The role required by a joining cluster member
jgroups-core.sasl.property=A protocol property with name and value.
jgroups-core.sasl.add=Adds the SASL protocol
jgroups-core.sasl.remove=Removes the SASL protocol

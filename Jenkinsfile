#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('Cleanup') {
            steps{
                echo "Do not allow changing scripts from strangers!!!"
            }
        }
    }
}
